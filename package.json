{
  "name": "open-scope-io",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm --filter demo dev",
    "test": "vitest run",
    "typecheck": "pnpm -r run typecheck",
    "prepare:goldens": "tsx scripts/autoresearch/prepare.ts"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.0",
    "sharp": "^0.33.5",
    "tsup": "^8.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=18"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "sharp"
    ]
  }
}